# 📓 Day 98: Advanced Exception Handling & Logging

> **⚠️ EDUCATIONAL PURPOSE ONLY:**
> This phase upgrades the script to production standards by replacing standard console outputs with robust `Logging` and structured `Exception Handling`.

### 🧠 Core Concepts Demonstrated
* **System Logging:** Utilizing Python's built-in `logging` module to track system behavior, replacing transient `print()` statements.
* **Log Levels:** Implementing various severity levels (`INFO`, `ERROR`, `CRITICAL`) to categorize system events.
* **Fail-Safe Execution:** Anticipating critical failures (like missing directories) and handling them gracefully without abrupt crashes.
* **Audit Trails:** Generating a timestamped `system.log` file, mimicking enterprise-level `Backend` environments.

### 🚀 Usage

Run the script:
```bash
python main.py